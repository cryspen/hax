-- Hand-written stub. Aeneas emits `FunsExternal_Template.lean` listing axioms
-- for items whose bodies it can't see; those already live in the parent
-- `CoreModels.RustPrimitives` (pulled in by `import CoreModels` from the
-- generated `Funs.lean`). This file just needs to exist to satisfy the
-- generated `import ClientTest.Extraction.FunsExternal`.
import Aeneas

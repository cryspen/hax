(* File automatically generated by Hacspec *)
Set Warnings "-notation-overridden,-ambiguous-paths".
From Crypt Require Import choice_type Package Prelude.
Import PackageNotation.
From extructures Require Import ord fset.
From mathcomp Require Import word_ssrZ word.
(* From Jasmin Require Import word. *)

From Coq Require Import ZArith.
From Coq Require Import Strings.String.
Import List.ListNotations.
Open Scope list_scope.
Open Scope Z_scope.
Open Scope bool_scope.

From Hacspec Require Import ChoiceEquality.
From Hacspec Require Import LocationUtility.
From Hacspec Require Import Hacspec_Lib_Comparable.
From Hacspec Require Import Hacspec_Lib_Pre.
From Hacspec Require Import Hacspec_Lib.

Open Scope hacspec_scope.
Import choice.Choice.Exports.

From RecordUpdate Require Import RecordUpdate.

Import RecordSetNotations.

Obligation Tactic := (* try timeout 8 *) solve_ssprove_obligations.

(*Not implemented yet? todo(item)*)

Definition t_Never : choice_type :=
  ⊥.

Equations t_Never_cast_to_repr (x : both t_Never) : both t_Never :=
  t_Never_cast_to_repr x  :=
    matchb x with
    end : both t_Never.
Fail Next Obligation.

Equations impl_Never__bar (self : both t_Never) : both 'unit :=
  impl_Never__bar self  :=
    never_to_any (matchb self with
    end) : both 'unit.
Fail Next Obligation.

Equations foo2 (never : both t_Never) : both (t_Failure (both 'bool)) :=
  foo2 never  :=
    failure (ret_both (something is not implemented yet.
Got type `Coroutine`: coroutines are not supported by hax

This is discussed in issue https://github.com/hacspec/hax/issues/924.
Please upvote or comment this issue if you see this error message.
Note: the error was labeled with context `AST import`.
 : chString)) (ret_both ( : chString)) : both (t_Failure (both 'bool)).
Fail Next Obligation.

Equations make (_ : both 'unit) : both (t_Option t_Never) :=
  make _  :=
    Option_None : both (t_Option t_Never).
Fail Next Obligation.

Equations impl_Never__foo (self : both t_Never) : both 'unit :=
  impl_Never__foo self  :=
    letb _ := never_to_any (matchb self with
    end) in
    never_to_any (impl__map make (fun never =>
      matchb never with
      end)) : both 'unit.
Fail Next Obligation.

Equations main (_ : both 'unit) : both 'unit :=
  main _  :=
    ret_both (tt : 'unit) : both 'unit.
Fail Next Obligation.

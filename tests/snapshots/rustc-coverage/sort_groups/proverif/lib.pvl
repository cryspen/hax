(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__sort_groups__generic_fn(cond : bool) =
       if cond
       then
         (let args = ((core_models__any__type_name(()))) in
          let args = ([core_models__fmt__rt__impl__new_display(args.0)]) in
          let wildcard2: bitstring = (
            std__io__stdio__e_print(
              core_models__fmt__rt__impl_1__new_v1(["","
"], args)
            )
          ) in
          let wildcard1: bitstring = (()) in
          ())
       else (()).

letfun new_tests__rustc_coverage__sort_groups__other_fn(wildcard3 : bitstring) =
       ().

letfun new_tests__rustc_coverage__sort_groups__main(wildcard10 : bitstring) =
       let cond = (
         core_models__cmp__f_gt(
           core_models__iter__traits__exact_size__f_len(std__env__args(())), 1
         )
       ) in
       let wildcard9: bitstring = (
         new_tests__rustc_coverage__sort_groups__generic_fn(cond)
       ) in
       let wildcard8: bitstring = (
         new_tests__rustc_coverage__sort_groups__generic_fn(
           core_models__ops__bit__f_not(cond)
         )
       ) in
       let wildcard7: bitstring = (
         if core_models__hint__black_box(false)
         then
           (let wildcard6: bitstring = (
              new_tests__rustc_coverage__sort_groups__generic_fn(cond)
            ) in
            ())
         else (())
       ) in
       let wildcard5: bitstring = (
         new_tests__rustc_coverage__sort_groups__generic_fn(cond)
       ) in
       let wildcard4: bitstring = (
         new_tests__rustc_coverage__sort_groups__other_fn(())
       ) in
       ().


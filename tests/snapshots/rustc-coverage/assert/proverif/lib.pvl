(*
  Run with:
    proverif -lib <hax>/hax-lib/proof-libs/proverif/primitives.pvl lib.pvl

  The `primitives.pvl` library ships with hax and supplies
  everything the extracted file references that isn't defined
  below: the public channel `c`, the `construct_fail` sink,
  `bitstring_default`/`bitstring_err`, the `Some`/`None`/
  `True`/`False`/`nat_lit` constructors, `logical_and`/`or`, and
  every `rust_primitives::*` / `core::*` / `hax_lib::*` opaque
  function the extraction surface needs.

  Any symbol this file references but does NOT define, and that is
  not in `primitives.pvl`, is listed in the companion
  `missingdecl.pvl`. That file is a DIAGNOSTIC, not part of the
  model: each entry must be supplied by hand or by another crate's
  extraction (e.g. a separately-extracted dependency). The goal is
  for `missingdecl.pvl` to be EMPTY — anything in it is a reachable
  definition that was silently stubbed out.
*)

(* src: src/rustc-coverage/assert.rs:7 new_tests__rustc_coverage__assert__might_fail_assert *)
(*  @fail(extraction): ssprove(HAX0001) *)
letfun new_tests__rustc_coverage__assert__might_fail_assert(one_plus_one: bitstring) =
  let wildcard: bitstring = std__io__stdio__u_print(
    core__fmt__rt__Impl_1__new_v1(
      rust_primitives__hax__array_cons(string_lit__does_x201_x20_x2b_x201_x20_x3d_x20, rust_primitives__hax__array_cons(string_lit___x3f_x0a, rust_primitives__hax__array_nil())),
      rust_primitives__hax__array_cons(core__fmt__rt__Impl__new_display(
        rust_primitives__hax__Tuple1__Tuple1__0(rust_primitives__hax__Tuple1__Tuple1(one_plus_one))
      ), rust_primitives__hax__array_nil())
    )
  ) in
  let wildcard: bitstring = rust_primitives__hax__Tuple0__Tuple0 in
  let wildcard: bitstring = (let rust_primitives__hax__Tuple2__Tuple2(
    left_val: bitstring,
    right_val: bitstring
  ) = rust_primitives__hax__Tuple2__Tuple2(
    rust_primitives__hax__machine_int__add(nat_lit(1), nat_lit(1)),
    one_plus_one
  ) in
    (let (=True()) = core__ops__bit__Not__not_kw(
        rust_primitives__hax__machine_int__eq(left_val, right_val)
      )
      in bitstring_err()
      else rust_primitives__hax__Tuple0__Tuple0)
  else bitstring_err()) in
  rust_primitives__hax__Tuple0__Tuple0.
(* src: src/rustc-coverage/assert.rs:13 new_tests__rustc_coverage__assert__main *)
(*  @fail(extraction): ssprove(HAX0001), coq(HAX0001, HAX0001) *)
letfun new_tests__rustc_coverage__assert__main(wildcard: bitstring) =
  let countdown_3 = (bitstring_err() (* Loops not supported in ProVerif *)) in
  rust_primitives__hax__Tuple0__Tuple0().
(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__assert__might_fail_assert(one_plus_one : nat) =
       let args = ((one_plus_one)) in
       let args = ([core_models__fmt__rt__impl__new_display(args.0)]) in
       let wildcard3: bitstring = (
         std__io__stdio__e_print(
           core_models__fmt__rt__impl_1__new_v1(["does 1 + 1 = ","?
"], args)
         )
       ) in
       let wildcard2: bitstring = (()) in
       let wildcard1: bitstring = (
         let (left_val: nat, right_val: nat) = (
           core_models__ops__arith__f_add(1, 1), one_plus_one
         ) in (
           if
             core_models__ops__bit__f_not(
                 core_models__cmp__f_eq(left_val, right_val)
               )
           then
             (let kind = (core_models__panicking__AssertKind_Eq()) in
              bitstring_err())
           else (())
         )
       ) in
       ().

letfun new_tests__rustc_coverage__assert__main(wildcard4 : bitstring) =
       let countdown = (10) in
       let countdown = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 (loop {
 |countdown| {
 (if core_models::cmp::f_gt(countdown, 0) {
 {
 let _: tuple0 = {
 (if core_models::cmp::f_eq(countdown, 1) {
 {
 let _: tuple0 = {
 new_tests::rustc_coverage__assert::might_..."
         )
       ) in
       ().


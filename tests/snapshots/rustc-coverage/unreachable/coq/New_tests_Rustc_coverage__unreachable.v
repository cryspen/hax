(* File automatically generated by Hacspec *)
From Coq Require Import ZArith.
Require Import List.
Import List.ListNotations.
Open Scope Z_scope.
Open Scope bool_scope.
Require Import Ascii.
Require Import String.
Require Import Coq.Floats.Floats.
From RecordUpdate Require Import RecordSet.
Import RecordSetNotations.
From Core Require Import Core.

(* NotImplementedYet *)







Definition v_UNREACHABLE_CLOSURE : unit -> unit :=
  fun _ =>
    failure (("Explicit rejection by a phase in the Hax engine:
a node of kind [Unsafe] have been found in the AST

Note: the error was labeled with context `reject_Unsafe`.
"%string : string)) (("unsafe {
 rust_primitives::hax::never_to_any(
 core_models::hint::unreachable_unchecked(Tuple0),
 )
 }"%string : string)).

Definition unreachable_function '(_ : unit) : unit :=
  failure (("Explicit rejection by a phase in the Hax engine:
a node of kind [Unsafe] have been found in the AST

Note: the error was labeled with context `reject_Unsafe`.
"%string : string)) (("unsafe {
 rust_primitives::hax::never_to_any(
 core_models::hint::unreachable_unchecked(Tuple0),
 )
 }"%string : string)).

Definition unreachable_intrinsic '(_ : unit) : unit :=
  failure (("Explicit rejection by a phase in the Hax engine:
a node of kind [Unsafe] have been found in the AST

Note: the error was labeled with context `reject_Unsafe`.
"%string : string)) (("unsafe {
 rust_primitives::hax::never_to_any(core_models::intrinsics::unreachable(Tuple0))
 }"%string : string)).

Definition main '(_ : unit) : unit :=
  let _ := if
    black_box ((false : bool))
  then
    let _ := v_UNREACHABLE_CLOSURE (tt) in
    tt
  else
    tt in
  let _ := if
    black_box ((false : bool))
  then
    let _ := unreachable_function (tt) in
    tt
  else
    tt in
  if
    black_box ((false : bool))
  then
    let _ := unreachable_intrinsic (tt) in
    tt
  else
    tt.

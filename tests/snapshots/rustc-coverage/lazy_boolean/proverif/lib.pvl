(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__lazy_boolean__main(wildcard1 : bitstring) =
       let is_true = (
         core_models__cmp__f_eq(
           core_models__iter__traits__exact_size__f_len(std__env__args(())), 1
         )
       ) in
       let (a: nat, b: nat, c: nat) = ((0, 0, 0)) in
       let (a: nat, b: nat, c: nat) = (
         if is_true
         then
           (let a = (1) in
            let b = (10) in
            let c = (100) in
            (a, b, c))
         else ((a, b, c))
       ) in
       let somebool = (
         core_models__cmp__f_lt(a, b) || core_models__cmp__f_lt(b, c)
       ) in
       let somebool = (
         core_models__cmp__f_lt(b, a) || core_models__cmp__f_lt(b, c)
       ) in
       let somebool = (
         core_models__cmp__f_lt(a, b) && core_models__cmp__f_lt(b, c)
       ) in
       let somebool = (
         core_models__cmp__f_lt(b, a) && core_models__cmp__f_lt(b, c)
       ) in
       let a = (
         if core_models__ops__bit__f_not(is_true)
         then
           (let a = (2) in
            a)
         else (a)
       ) in
       let (b: nat, c: nat) = (
         if is_true
         then
           (let b = (30) in
            (b, c))
         else (
           let c = (400) in
           (b, c)
         )
       ) in
       let a = (
         if core_models__ops__bit__f_not(is_true)
         then
           (let a = (2) in
            a)
         else (a)
       ) in
       if is_true
       then
         (let b = (30) in
          ())
       else (
         let c = (400) in
         ()
       ).


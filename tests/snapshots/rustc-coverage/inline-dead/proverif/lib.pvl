(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__inline_dead__dead(wildcard1 : bitstring) =
       42.

letfun new_tests__rustc_coverage__inline_dead__live(wildcard2 : bitstring) =
       if v_B then (new_tests__rustc_coverage__inline_dead__dead(())) else (0).

letfun new_tests__rustc_coverage__inline_dead__main(wildcard8 : bitstring) =
       let args = ((new_tests__rustc_coverage__inline_dead__live(()))) in
       let args = ([core_models__fmt__rt__impl__new_display(args.0)]) in
       let wildcard7: bitstring = (
         std__io__stdio__e_print(
           core_models__fmt__rt__impl_1__new_v1(["","
"], args)
         )
       ) in
       let wildcard6: bitstring = (()) in
       let f = (
         |x|let wildcard5: bitstring = (
           if true
           then
             (let wildcard4: bitstring = (
                if core_models__ops__bit__f_not(x)
                then (bitstring_err())
                else (())
              ) in
              ())
           else (())
         ) in
         ()
       ) in
       let wildcard3: bitstring = (
         core_models__ops__function__f_call(f, (false))
       ) in
       ().


(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__fn_sig_into_try__a(wildcard2 : bitstring) =
       let wildcard1: bitstring = (Some(nat_to_bitstring(7))) in
       Some(nat_to_bitstring(0)).

letfun new_tests__rustc_coverage__fn_sig_into_try__b(wildcard4 : bitstring) =
       let Some(nat_to_bitstring(wildcard3: bitstring)) = Some(
         nat_to_bitstring(7)
       ) in Some(nat_to_bitstring(0))
       else let None() = Some(nat_to_bitstring(7)) in todo return;.

letfun new_tests__rustc_coverage__fn_sig_into_try__c(wildcard6 : bitstring) =
       let Some(nat_to_bitstring(wildcard5: bitstring)) = Some(
         nat_to_bitstring(7)
       ) in Some(nat_to_bitstring(0))
       else let None() = Some(nat_to_bitstring(7)) in todo return;.

letfun new_tests__rustc_coverage__fn_sig_into_try__d(wildcard9 : bitstring) =
       let wildcard8: bitstring:bitstring = (()) in
       let Some(nat_to_bitstring(wildcard7: bitstring)) = Some(
         nat_to_bitstring(7)
       ) in Some(nat_to_bitstring(0))
       else let None() = Some(nat_to_bitstring(7)) in todo return;.

letfun new_tests__rustc_coverage__fn_sig_into_try__main(wildcard14 : bitstring) =
       let wildcard13: bitstring = (
         new_tests__rustc_coverage__fn_sig_into_try__a(())
       ) in
       let wildcard12: bitstring = (
         new_tests__rustc_coverage__fn_sig_into_try__b(())
       ) in
       let wildcard11: bitstring = (
         new_tests__rustc_coverage__fn_sig_into_try__c(())
       ) in
       let wildcard10: bitstring = (
         new_tests__rustc_coverage__fn_sig_into_try__d(())
       ) in
       ().


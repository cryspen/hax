(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__inline__length(xs : bitstring) =
       core_models__slice__impl__len(xs).

letfun new_tests__rustc_coverage__inline__swap(xs : bitstring, i : nat, j : nat) =
       let t = (core_models__ops__index__f_index(xs, i)) in
       let xs = (
         rust_primitives__hax__monomorphized_update_at__update_at_usize(
           xs, i, core_models__ops__index__f_index(xs, j)
         )
       ) in
       let xs = (
         rust_primitives__hax__monomorphized_update_at__update_at_usize(xs, j, t
         )
       ) in
       xs.

letfun new_tests__rustc_coverage__inline__display(xs : bitstring) =
       let wildcard3: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for x in (core_models::iter::traits::collect::f_into_iter(xs)) {
 {
 let args: tuple1<T> = { Tuple1(x) };
 {
 let args: [core_models::fmt::rt::t_Argument; 1] = {
 [
 core_models::fmt::rt::impl__new..."
         )
       ) in
       let wildcard2: bitstring = (
         std__io__stdio__e_print(core_models__fmt__rt__impl_1__new_const(["
"]))
       ) in
       let wildcard1: bitstring = (()) in
       ().

letfun new_tests__rustc_coverage__inline__error(wildcard4 : bitstring) =
       bitstring_err().

letfun new_tests__rustc_coverage__inline__permutate(xs : bitstring, k : nat) =
       let n = (new_tests__rustc_coverage__inline__length(xs)) in
       let xs = (
         if core_models__cmp__f_eq(k, n)
         then
           (let wildcard5: bitstring = (
              new_tests__rustc_coverage__inline__display(xs)
            ) in
            xs)
         else (
           if core_models__cmp__f_lt(k, n)
           then
             (rust_primitives__hax__failure(
                "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
                "{
 (for i in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: k,
 f_end: n,
 })) {
 |xs| {
 {
 let xs: [T] = {
 new_tests::rustc_coverage__inline::swap::<T>(..."
              ))
           else (
             let wildcard6: bitstring = (
               new_tests__rustc_coverage__inline__error(())
             ) in
             xs
           )
         )
       ) in
       xs.

letfun new_tests__rustc_coverage__inline__permutations(xs : bitstring) =
       let ys = (alloc__borrow__f_to_owned(xs)) in
       let ys = (
         alloc__slice__impl__to_vec(
           new_tests__rustc_coverage__inline__permutate(
             alloc__vec__impl_1__as_slice(ys), 0
           )
         )
       ) in
       ().

letfun new_tests__rustc_coverage__inline__main(wildcard8 : bitstring) =
       let wildcard7: bitstring = (
         new_tests__rustc_coverage__inline__permutations([`a`,`b`,`c`])
       ) in
       ().


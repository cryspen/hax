(* File automatically generated by Hacspec *)
Set Warnings "-notation-overridden,-ambiguous-paths".
From Crypt Require Import choice_type Package Prelude.
Import PackageNotation.
From extructures Require Import ord fset.
From mathcomp Require Import word_ssrZ word.
(* From Jasmin Require Import word. *)

From Coq Require Import ZArith.
From Coq Require Import Strings.String.
Import List.ListNotations.
Open Scope list_scope.
Open Scope Z_scope.
Open Scope bool_scope.

From Hacspec Require Import ChoiceEquality.
From Hacspec Require Import LocationUtility.
From Hacspec Require Import Hacspec_Lib_Comparable.
From Hacspec Require Import Hacspec_Lib_Pre.
From Hacspec Require Import Hacspec_Lib.

Open Scope hacspec_scope.
Import choice.Choice.Exports.

From RecordUpdate Require Import RecordUpdate.

Import RecordSetNotations.

Obligation Tactic := (* try timeout 8 *) solve_ssprove_obligations.

(*Not implemented yet? todo(item)*)

Equations length {v_T : v_T} (xs : both (seq v_T)) : both uint_size :=
  length xs  :=
    impl__len xs : both uint_size.
Fail Next Obligation.

Equations swap {v_T : v_T} `{ t_Copy v_T} (xs : both (seq v_T)) (i : both uint_size) (j : both uint_size) : both (seq v_T) :=
  swap xs i j  :=
    letb t := xs.a[i] in
    letb xs := update_at_usize xs i (xs.a[j]) in
    letb xs := update_at_usize xs j t in
    xs : both (seq v_T).
Fail Next Obligation.

(*item error backend*)

Equations error (_ : both 'unit) : both 'unit :=
  error _  :=
    never_to_any (panic_fmt (impl_1__new_const (array_from_list [ret_both (error : chString)]))) : both 'unit.
Fail Next Obligation.

Equations permutate {v_T : v_T} `{ t_Copy v_T} `{ t_Display v_T} (xs : both (seq v_T)) (k : both uint_size) : both (seq v_T) :=
  permutate xs k  :=
    letb n := length xs in
    letb xs := ifb k =.? n
    then letb _ := display xs in
    xs
    else ifb k <.? n
    then foldi_both_list (f_into_iter (Build_t_Range (f_start := k) (f_end := n))) (fun i =>
      ssp (fun xs =>
        letb xs := swap xs i k in
        letb xs := permutate xs (k .+ (ret_both (1 : uint_size))) in
        letb xs := swap xs i k in
        xs : (both (seq v_T)))) xs
    else letb _ := error in
    xs in
    xs : both (seq v_T).
Fail Next Obligation.

Equations permutations {v_T : v_T} `{ t_Copy v_T} `{ t_Display v_T} (xs : both (seq v_T)) : both 'unit :=
  permutations xs  :=
    letb ys := f_to_owned xs in
    letb ys := impl__to_vec (permutate (impl_1__as_slice ys) (ret_both (0 : uint_size))) in
    ret_both (tt : 'unit) : both 'unit.
Fail Next Obligation.

(*item error backend*)

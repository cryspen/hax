(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__closure_unit_return__explicit_unit(
         wildcard4 : bitstring
       ) =
       let closure = (
         |wildcard2: bitstring|let wildcard3: bitstring = (()) in
         ()
       ) in
       let wildcard1: bitstring = (core_models__mem__drop(closure)) in
       ().

letfun new_tests__rustc_coverage__closure_unit_return__implicit_unit(
         wildcard8 : bitstring
       ) =
       let closure = (
         |wildcard6: bitstring|let wildcard7: bitstring = (()) in
         ()
       ) in
       let wildcard5: bitstring = (core_models__mem__drop(closure)) in
       ().

letfun new_tests__rustc_coverage__closure_unit_return__main(
         wildcard11 : bitstring
       ) =
       let wildcard10: bitstring = (
         new_tests__rustc_coverage__closure_unit_return__explicit_unit(())
       ) in
       let wildcard9: bitstring = (
         new_tests__rustc_coverage__closure_unit_return__implicit_unit(())
       ) in
       ().


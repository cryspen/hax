(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__long_and_wide__wide_function(
         wildcard2 : bitstring
       ) =
       let wildcard1: bitstring = (()) in
       ().

letfun new_tests__rustc_coverage__long_and_wide__long_function(
         wildcard3 : bitstring
       ) =
       ().

letfun new_tests__rustc_coverage__long_and_wide__far_function(
         wildcard4 : bitstring
       ) =
       ().

letfun new_tests__rustc_coverage__long_and_wide__main(wildcard8 : bitstring) =
       let wildcard7: bitstring = (
         new_tests__rustc_coverage__long_and_wide__wide_function(())
       ) in
       let wildcard6: bitstring = (
         new_tests__rustc_coverage__long_and_wide__long_function(())
       ) in
       let wildcard5: bitstring = (
         new_tests__rustc_coverage__long_and_wide__far_function(())
       ) in
       ().


(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__match_or_pattern__main(wildcard4 : bitstring) =
       let is_true = (
         core_models__cmp__f_eq(
           core_models__iter__traits__exact_size__f_len(std__env__args(())), 1
         )
       ) in
       let a:nat = (0) in
       let b:nat = (0) in
       let (a: nat, b: nat) = (
         if is_true
         then
           (let a = (2) in
            let b = (0) in
            (a, b))
         else ((a, b))
       ) in
       let wildcard3: bitstring = (
         let (=0| =1, =2| =3) = (a, b) in ()
         else ()
       ) in
       let (a: nat, b: nat) = (
         if is_true
         then
           (let a = (0) in
            let b = (0) in
            (a, b))
         else ((a, b))
       ) in
       let wildcard2: bitstring = (
         let (=0| =1, =2| =3) = (a, b) in ()
         else ()
       ) in
       let (a: nat, b: nat) = (
         if is_true
         then
           (let a = (2) in
            let b = (2) in
            (a, b))
         else ((a, b))
       ) in
       let wildcard1: bitstring = (
         let (=0| =1, =2| =3) = (a, b) in ()
         else ()
       ) in
       let (a: nat, b: nat) = (
         if is_true
         then
           (let a = (0) in
            let b = (2) in
            (a, b))
         else ((a, b))
       ) in
       let (=0| =1, =2| =3) = (a, b) in ()
       else ().


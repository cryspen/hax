(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__closure_bug__main(wildcard16 : bitstring) =
       let truthy = (
         core_models__cmp__f_eq(
           core_models__iter__traits__exact_size__f_len(std__env__args(())), 1
         )
       ) in
       let a = (|wildcard15: bitstring|if truthy then (true) else (false)) in
       let wildcard14: bitstring = (core_models__ops__function__f_call(a, ())) in
       let wildcard13: bitstring = (
         if truthy
         then
           (let wildcard12: bitstring = (
              core_models__ops__function__f_call(a, ())
            ) in
            ())
         else (())
       ) in
       let b = (|wildcard11: bitstring|if truthy then (true) else (false)) in
       let wildcard10: bitstring = (core_models__ops__function__f_call(b, ())) in
       let wildcard9: bitstring = (
         if truthy
         then
           (let wildcard8: bitstring = (
              core_models__ops__function__f_call(b, ())
            ) in
            ())
         else (())
       ) in
       let c = (|wildcard7: bitstring|if truthy then (true) else (false)) in
       let wildcard6: bitstring = (core_models__ops__function__f_call(c, ())) in
       let wildcard5: bitstring = (
         if truthy
         then
           (let wildcard4: bitstring = (
              core_models__ops__function__f_call(c, ())
            ) in
            ())
         else (())
       ) in
       let d = (|wildcard3: bitstring|if truthy then (true) else (false)) in
       let wildcard2: bitstring = (core_models__ops__function__f_call(d, ())) in
       if truthy
       then
         (let wildcard1: bitstring = (core_models__ops__function__f_call(d, ())) in
          ())
       else (()).


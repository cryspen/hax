(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__branch__if__say(message : bitstring) =
       let wildcard1: bitstring = (core_models__hint__black_box(message)) in
       ().

letfun new_tests__rustc_coverage__branch__if__branch_not(a : bool) =
       let wildcard8: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let wildcard7: bitstring = (
         if a then (new_tests__rustc_coverage__branch__if__say("a")) else (())
       ) in
       let wildcard6: bitstring = (
         if core_models__ops__bit__f_not(a)
         then
           (let wildcard5: bitstring = (
              new_tests__rustc_coverage__branch__if__say("not a")
            ) in
            ())
         else (())
       ) in
       let wildcard4: bitstring = (
         if core_models__ops__bit__f_not(core_models__ops__bit__f_not(a))
         then
           (let wildcard3: bitstring = (
              new_tests__rustc_coverage__branch__if__say("not not a")
            ) in
            ())
         else (())
       ) in
       if
         core_models__ops__bit__f_not(
             core_models__ops__bit__f_not(core_models__ops__bit__f_not(a))
           )
       then
         (let wildcard2: bitstring = (
            new_tests__rustc_coverage__branch__if__say("not not not a")
          ) in
          ())
       else (()).

letfun new_tests__rustc_coverage__branch__if__branch_not_as(a : bool) =
       let wildcard14: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let wildcard13: bitstring = (
         if core_models__ops__bit__f_not(a)
         then
           (let wildcard12: bitstring = (
              new_tests__rustc_coverage__branch__if__say("not (a as bool)")
            ) in
            ())
         else (())
       ) in
       let wildcard11: bitstring = (
         if core_models__ops__bit__f_not(core_models__ops__bit__f_not(a))
         then
           (let wildcard10: bitstring = (
              new_tests__rustc_coverage__branch__if__say("not not (a as bool)")
            ) in
            ())
         else (())
       ) in
       if
         core_models__ops__bit__f_not(
             core_models__ops__bit__f_not(core_models__ops__bit__f_not(a))
           )
       then
         (let wildcard9: bitstring = (
            new_tests__rustc_coverage__branch__if__say("not not (a as bool)")
          ) in
          ())
       else (()).

letfun new_tests__rustc_coverage__branch__if__branch_and(a : bool, b : bool) =
       let wildcard17: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       if a && b
       then
         (let wildcard15: bitstring = (
            new_tests__rustc_coverage__branch__if__say("both")
          ) in
          ())
       else (
         let wildcard16: bitstring = (
           new_tests__rustc_coverage__branch__if__say("not both")
         ) in
         ()
       ).

letfun new_tests__rustc_coverage__branch__if__branch_or(a : bool, b : bool) =
       let wildcard20: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       if a || b
       then
         (let wildcard18: bitstring = (
            new_tests__rustc_coverage__branch__if__say("either")
          ) in
          ())
       else (
         let wildcard19: bitstring = (
           new_tests__rustc_coverage__branch__if__say("neither")
         ) in
         ()
       ).

letfun new_tests__rustc_coverage__branch__if__main(wildcard22 : bitstring) =
       let wildcard21: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for a in (core_models::iter::traits::collect::f_into_iter([false, true, true])) {
 {
 let _: tuple0 = { new_tests::rustc_coverage__branch__if::branch_not(a) };
 {
 let _: tuple0 = {
 new_tests::rus..."
         )
       ) in
       (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for a in (core_models::iter::traits::collect::f_into_iter([
 false, true, true, true, true,
 ])) {
 {
 for b in (core_models::iter::traits::collect::f_into_iter([
 false, true, true,
 ])) {
 {
 let..."
         ),
         ()
       ).


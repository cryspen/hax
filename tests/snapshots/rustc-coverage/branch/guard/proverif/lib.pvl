(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__branch__guard__branch_match_guard(x : Option) =
       let wildcard1: bitstring = (
         rust_primitives__hax__failure(
           "[hax::opaque] Explicit rejection by a phase in the Hax engine: a node of kind [Loop] have been found in the AST",
           ""
         )
       ) in
       rust_primitives__hax__failure(
         "[hax::opaque] Explicit rejection by a phase in the Hax engine: a node of kind [Match_guard] have been found in the AST",
         ""
       ).

letfun new_tests__rustc_coverage__branch__guard__main(wildcard6 : bitstring) =
       let wildcard5: bitstring = (
         new_tests__rustc_coverage__branch__guard__branch_match_guard(
           Some(nat_to_bitstring(0))
         )
       ) in
       let wildcard4: bitstring = (
         new_tests__rustc_coverage__branch__guard__branch_match_guard(
           Some(nat_to_bitstring(2))
         )
       ) in
       let wildcard3: bitstring = (
         new_tests__rustc_coverage__branch__guard__branch_match_guard(
           Some(nat_to_bitstring(6))
         )
       ) in
       let wildcard2: bitstring = (
         new_tests__rustc_coverage__branch__guard__branch_match_guard(
           Some(nat_to_bitstring(3))
         )
       ) in
       ().


(* File automatically generated by Hacspec *)
Set Warnings "-notation-overridden,-ambiguous-paths".
From Crypt Require Import choice_type Package Prelude.
Import PackageNotation.
From extructures Require Import ord fset.
From mathcomp Require Import word_ssrZ word.
(* From Jasmin Require Import word. *)

From Coq Require Import ZArith.
From Coq Require Import Strings.String.
Import List.ListNotations.
Open Scope list_scope.
Open Scope Z_scope.
Open Scope bool_scope.

From Hacspec Require Import ChoiceEquality.
From Hacspec Require Import LocationUtility.
From Hacspec Require Import Hacspec_Lib_Comparable.
From Hacspec Require Import Hacspec_Lib_Pre.
From Hacspec Require Import Hacspec_Lib.

Open Scope hacspec_scope.
Import choice.Choice.Exports.

From RecordUpdate Require Import RecordUpdate.

Import RecordSetNotations.

Obligation Tactic := (* try timeout 8 *) solve_ssprove_obligations.

(*Not implemented yet? todo(item)*)

(*Not implemented yet? todo(item)*)

Equations branch_match_guard (x : both (t_Option int32)) : both 'unit :=
  branch_match_guard x  :=
    letb _ := foldi_both_list (f_into_iter (Build_t_Range (f_start := ret_both (0 : int32)) (f_end := ret_both (1 : int32)))) (fun _ =>
      ssp (fun _ =>
        ret_both (tt : 'unit) : (both 'unit))) (ret_both (tt : 'unit)) in
    matchb x with
    | Option_Some_case 0 =>
      letb 0 := ret_both ((0) : (int32)) in
      letb _ := e_print (impl_1__new_const (array_from_list [ret_both (zero
 : chString)])) in
      letb _ := ret_both (tt : 'unit) in
      ret_both (tt : 'unit)
    | _ =>
      matchb matchb x with
      | Option_Some_case x =>
        letb x := ret_both ((x) : (int32)) in
        matchb (x .% (ret_both (2 : int32))) =.? (ret_both (0 : int32)) with
        | true =>
          letb _ := e_print (impl_1__new_const (array_from_list [ret_both (is nonzero and even
 : chString)])) in
          letb _ := ret_both (tt : 'unit) in
          Option_Some (ret_both (tt : 'unit))
        | _ =>
          Option_None
        end
      | _ =>
        Option_None
      end with
      | Option_Some_case x =>
        letb x := ret_both ((x) : ('unit)) in
        x
      | Option_None_case  =>
        matchb matchb x with
        | Option_Some_case x =>
          letb x := ret_both ((x) : (int32)) in
          matchb (x .% (ret_both (3 : int32))) =.? (ret_both (0 : int32)) with
          | true =>
            letb _ := e_print (impl_1__new_const (array_from_list [ret_both (is nonzero and odd, but divisible by 3
 : chString)])) in
            letb _ := ret_both (tt : 'unit) in
            Option_Some (ret_both (tt : 'unit))
          | _ =>
            Option_None
          end
        | _ =>
          Option_None
        end with
        | Option_Some_case x =>
          letb x := ret_both ((x) : ('unit)) in
          x
        | Option_None_case  =>
          letb _ := e_print (impl_1__new_const (array_from_list [ret_both (something else
 : chString)])) in
          letb _ := ret_both (tt : 'unit) in
          ret_both (tt : 'unit)
        end
      end
    end : both 'unit.
Fail Next Obligation.

Equations main (_ : both 'unit) : both 'unit :=
  main _  :=
    letb _ := branch_match_guard (Option_Some (ret_both (0 : int32))) in
    letb _ := branch_match_guard (Option_Some (ret_both (2 : int32))) in
    letb _ := branch_match_guard (Option_Some (ret_both (6 : int32))) in
    letb _ := branch_match_guard (Option_Some (ret_both (3 : int32))) in
    ret_both (tt : 'unit) : both 'unit.
Fail Next Obligation.

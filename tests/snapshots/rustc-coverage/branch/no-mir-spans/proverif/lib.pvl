(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__branch__no_mir_spans__while_cond(
         wildcard2 : bitstring
       ) =
       let wildcard1: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let a = (8) in
       (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 (loop {
 |a| {
 (if core_models::cmp::f_gt(a, 0) {
 {
 let a: int = { core_models::ops::arith::f_sub(a, 1) };
 a
 }
 } else {
 {
 #[note("rhs.typ=int")]
 let hoist1: rust_primitives::hax::t_Never =..."
         ),
         ()
       ).

letfun new_tests__rustc_coverage__branch__no_mir_spans__while_cond_not(
         wildcard4 : bitstring
       ) =
       let wildcard3: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let a = (8) in
       (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 (loop {
 |a| {
 (if core_models::ops::bit::f_not(core_models::cmp::f_eq(a, 0)) {
 {
 let a: int = { core_models::ops::arith::f_sub(a, 1) };
 a
 }
 } else {
 {
 #[note("rhs.typ=int")]
 let hoist3: r..."
         ),
         ()
       ).

letfun new_tests__rustc_coverage__branch__no_mir_spans__while_op_and(
         wildcard6 : bitstring
       ) =
       let wildcard5: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let a = (8) in
       let b = (4) in
       (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 (loop {
 |Tuple2(a, b)| {
 (if BinOp::Ast
 .And(core_models::cmp::f_gt(a, 0), core_models::cmp::f_gt(b, 0))
 {
 {
 let a: int = { core_models::ops::arith::f_sub(a, 1) };
 {
 let b: int = { core_mod..."
         ),
         ()
       ).

letfun new_tests__rustc_coverage__branch__no_mir_spans__while_op_or(
         wildcard8 : bitstring
       ) =
       let wildcard7: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let a = (4) in
       let b = (8) in
       (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 (loop {
 |Tuple2(a, b)| {
 (if BinOp::Ast
 .Or(core_models::cmp::f_gt(a, 0), core_models::cmp::f_gt(b, 0))
 {
 {
 let a: int = { core_models::ops::arith::f_sub(a, 1) };
 {
 let b: int = { core_mode..."
         ),
         ()
       ).

letfun new_tests__rustc_coverage__branch__no_mir_spans__main(
         wildcard13 : bitstring
       ) =
       let wildcard12: bitstring = (
         new_tests__rustc_coverage__branch__no_mir_spans__while_cond(())
       ) in
       let wildcard11: bitstring = (
         new_tests__rustc_coverage__branch__no_mir_spans__while_cond_not(())
       ) in
       let wildcard10: bitstring = (
         new_tests__rustc_coverage__branch__no_mir_spans__while_op_and(())
       ) in
       let wildcard9: bitstring = (
         new_tests__rustc_coverage__branch__no_mir_spans__while_op_or(())
       ) in
       ().


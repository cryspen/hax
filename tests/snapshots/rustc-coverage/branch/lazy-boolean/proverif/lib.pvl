(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__branch__lazy_boolean__branch_and(
         a : bool, b : bool
       ) =
       let wildcard2: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let c = (a && b) in
       let wildcard1: bitstring = (core_models__hint__black_box(c)) in
       ().

letfun new_tests__rustc_coverage__branch__lazy_boolean__branch_or(
         a : bool, b : bool
       ) =
       let wildcard4: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let c = (a || b) in
       let wildcard3: bitstring = (core_models__hint__black_box(c)) in
       ().

letfun new_tests__rustc_coverage__branch__lazy_boolean__chain(x : nat) =
       let wildcard7: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let c = (
         core_models__cmp__f_gt(x, 1) && core_models__cmp__f_gt(x, 2) && core_models__cmp__f_gt(
           x, 4
         ) && core_models__cmp__f_gt(x, 8)
       ) in
       let wildcard6: bitstring = (core_models__hint__black_box(c)) in
       let d = (
         core_models__cmp__f_lt(x, 1) || core_models__cmp__f_lt(x, 2) || core_models__cmp__f_lt(
           x, 4
         ) || core_models__cmp__f_lt(x, 8)
       ) in
       let wildcard5: bitstring = (core_models__hint__black_box(d)) in
       ().

letfun new_tests__rustc_coverage__branch__lazy_boolean__nested_mixed(x : nat) =
       let wildcard10: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let c = (
         core_models__cmp__f_lt(x, 4) || core_models__cmp__f_ge(x, 9) && core_models__cmp__f_lt(
           x, 2
         ) || core_models__cmp__f_ge(x, 10)
       ) in
       let wildcard9: bitstring = (core_models__hint__black_box(c)) in
       let d = (
         core_models__cmp__f_lt(x, 4) && core_models__cmp__f_lt(x, 1) || core_models__cmp__f_ge(
           x, 8
         ) && core_models__cmp__f_ge(x, 10)
       ) in
       let wildcard8: bitstring = (core_models__hint__black_box(d)) in
       ().

letfun new_tests__rustc_coverage__branch__lazy_boolean__main(
         wildcard12 : bitstring
       ) =
       let wildcard11: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for a in (core_models::iter::traits::collect::f_into_iter([
 false, true, true, true, true,
 ])) {
 {
 for b in (core_models::iter::traits::collect::f_into_iter([
 false, true, true,
 ])) {
 {
 let..."
         )
       ) in
       (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for x in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 16,
 })) {
 {
 let _: tuple0 = {
 new_tests::rustc_coverage__branch__lazy_boolean::ch..."
         ),
         ()
       ).


(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__branch__if_let__say(message : bitstring) =
       let wildcard1: bitstring = (core_models__hint__black_box(message)) in
       ().

letfun new_tests__rustc_coverage__branch__if_let__if_let(input : Option) =
       let wildcard6: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 1,
 })) {
 Tuple0
 }
 }"
         )
       ) in
       let wildcard5: bitstring = (
         let Some(bitstring_to_bitstring(x)) = input in (
           let wildcard3: bitstring = (
             new_tests__rustc_coverage__branch__if_let__say(x)
           ) in
           ()
         )
         else (
           let wildcard4: bitstring = (
             new_tests__rustc_coverage__branch__if_let__say("none")
           ) in
           ()
         )
       ) in
       let wildcard2: bitstring = (
         new_tests__rustc_coverage__branch__if_let__say("done")
       ) in
       ().

letfun new_tests__rustc_coverage__branch__if_let__if_let_chain(
         a : Option, b : Option
       ) =
       let wildcard11: bitstring = (
         if
           rust_primitives__hax__failure(
               "something is not implemented yet.
Let-chains (e.g. `if let .. && let ..`) are not supported.

This is discussed in issue https://github.com/hacspec/hax/issues/2018.
Please upvote or comment this issue if you see this error message.
Note: the error was labeled with context `AST import`.
",
               ""
             ) && rust_primitives__hax__failure(
               "something is not implemented yet.
Let-chains (e.g. `if let .. && let ..`) are not supported.

This is discussed in issue https://github.com/hacspec/hax/issues/2018.
Please upvote or comment this issue if you see this error message.
Note: the error was labeled with context `AST import`.
",
               ""
             )
         then
           (let wildcard9: bitstring = (
              new_tests__rustc_coverage__branch__if_let__say(x)
            ) in
            let wildcard8: bitstring = (
              new_tests__rustc_coverage__branch__if_let__say(y)
            ) in
            ())
         else (
           let wildcard10: bitstring = (
             new_tests__rustc_coverage__branch__if_let__say("not both")
           ) in
           ()
         )
       ) in
       let wildcard7: bitstring = (
         new_tests__rustc_coverage__branch__if_let__say("done")
       ) in
       ().

letfun new_tests__rustc_coverage__branch__if_let__main(wildcard19 : bitstring) =
       let wildcard18: bitstring = (
         new_tests__rustc_coverage__branch__if_let__if_let(
           Some(bitstring_to_bitstring("x"))
         )
       ) in
       let wildcard17: bitstring = (
         new_tests__rustc_coverage__branch__if_let__if_let(
           Some(bitstring_to_bitstring("x"))
         )
       ) in
       let wildcard16: bitstring = (
         new_tests__rustc_coverage__branch__if_let__if_let(None())
       ) in
       let wildcard15: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 8,
 })) {
 new_tests::rustc_coverage__branch__if_let::if_let_chain(
 core_models::opt..."
         )
       ) in
       let wildcard14: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 4,
 })) {
 new_tests::rustc_coverage__branch__if_let::if_let_chain(
 core_models::opt..."
         )
       ) in
       let wildcard13: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _ in (core_models::iter::traits::collect::f_into_iter(core_models::ops::range::Range {
 f_start: 0,
 f_end: 2,
 })) {
 new_tests::rustc_coverage__branch__if_let::if_let_chain(
 core_models::opt..."
         )
       ) in
       let wildcard12: bitstring = (
         new_tests__rustc_coverage__branch__if_let__if_let_chain(None(), None())
       ) in
       ().


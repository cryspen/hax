(* File automatically generated by Hacspec *)
Set Warnings "-notation-overridden,-ambiguous-paths".
From Crypt Require Import choice_type Package Prelude.
Import PackageNotation.
From extructures Require Import ord fset.
From mathcomp Require Import word_ssrZ word.
(* From Jasmin Require Import word. *)

From Coq Require Import ZArith.
From Coq Require Import Strings.String.
Import List.ListNotations.
Open Scope list_scope.
Open Scope Z_scope.
Open Scope bool_scope.

From Hacspec Require Import ChoiceEquality.
From Hacspec Require Import LocationUtility.
From Hacspec Require Import Hacspec_Lib_Comparable.
From Hacspec Require Import Hacspec_Lib_Pre.
From Hacspec Require Import Hacspec_Lib.

Open Scope hacspec_scope.
Import choice.Choice.Exports.

From RecordUpdate Require Import RecordUpdate.

Import RecordSetNotations.

Obligation Tactic := (* try timeout 8 *) solve_ssprove_obligations.

(*Not implemented yet? todo(item)*)

(*Not implemented yet? todo(item)*)

Definition t_Uninhabited : choice_type :=
  ⊥.

Equations t_Uninhabited_cast_to_repr (x : both t_Uninhabited) : both t_Never :=
  t_Uninhabited_cast_to_repr x  :=
    matchb x with
    end : both t_Never.
Fail Next Obligation.

Definition t_Trivial : choice_type :=
  ('unit).
Notation "'Trivial_Value_case'" := tt (at level 100).
Equations Trivial_Value : both t_Trivial :=
  Trivial_Value  :=
    ret_both (tt : 'unit : t_Trivial) : both t_Trivial.
Fail Next Obligation.

Equations t_Trivial_cast_to_repr (x : both t_Trivial) : both uint_size :=
  t_Trivial_cast_to_repr x  :=
    matchb x with
    | Trivial_Value_case  =>
      ret_both (0 : uint_size)
    end : both uint_size.
Fail Next Obligation.

Equations consume {v_T : v_T} (x : both v_T) : both 'unit :=
  consume x  :=
    letb _ := black_box x in
    ret_both (tt : 'unit) : both 'unit.
Fail Next Obligation.

Equations e_uninhabited (x : both t_Uninhabited) : both 'unit :=
  e_uninhabited x  :=
    letb _ := foldi_both_list (f_into_iter (Build_t_Range (f_start := ret_both (0 : int32)) (f_end := ret_both (1 : int32)))) (fun _ =>
      ssp (fun _ =>
        ret_both (tt : 'unit) : (both 'unit))) (ret_both (tt : 'unit)) in
    letb _ := never_to_any (matchb x with
    end) in
    never_to_any (consume (ret_both (done : chString))) : both 'unit.
Fail Next Obligation.

Equations trivial (x : both t_Trivial) : both 'unit :=
  trivial x  :=
    letb _ := foldi_both_list (f_into_iter (Build_t_Range (f_start := ret_both (0 : int32)) (f_end := ret_both (1 : int32)))) (fun _ =>
      ssp (fun _ =>
        ret_both (tt : 'unit) : (both 'unit))) (ret_both (tt : 'unit)) in
    letb _ := matchb x with
    | Trivial_Value_case  =>
      consume (ret_both (trivial : chString))
    end in
    letb _ := consume (ret_both (done : chString)) in
    ret_both (tt : 'unit) : both 'unit.
Fail Next Obligation.

Equations main (_ : both 'unit) : both 'unit :=
  main _  :=
    letb _ := trivial Trivial_Value in
    ret_both (tt : 'unit) : both 'unit.
Fail Next Obligation.

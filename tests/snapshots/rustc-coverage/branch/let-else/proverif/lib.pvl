(*
  Run with:
    proverif -lib <hax>/hax-lib/proof-libs/proverif/primitives.pvl lib.pvl

  The `primitives.pvl` library ships with hax and supplies
  everything the extracted file references that isn't defined
  below: the public channel `c`, the `construct_fail` sink,
  `bitstring_default`/`bitstring_err`, the `Some`/`None`/
  `True`/`False`/`nat_lit` constructors, `logical_and`/`or`, and
  every `rust_primitives::*` / `core::*` / `hax_lib::*` opaque
  function the extraction surface needs.

  Any symbol this file references but does NOT define, and that is
  not in `primitives.pvl`, is listed in the companion
  `missingdecl.pvl`. That file is a DIAGNOSTIC, not part of the
  model: each entry must be supplied by hand or by another crate's
  extraction (e.g. a separately-extracted dependency). The goal is
  for `missingdecl.pvl` to be EMPTY — anything in it is a reachable
  definition that was silently stubbed out.
*)

(* src: src/rustc-coverage/branch/let-else.rs:25 new_tests__rustc_coverage__branch__let_else__say *)
letfun new_tests__rustc_coverage__branch__let_else__say(message: bitstring) =
  let wildcard: bitstring = core__hint__black_box(message) in
  rust_primitives__hax__Tuple0__Tuple0.
(* src: src/rustc-coverage/branch/let-else.rs:13 new_tests__rustc_coverage__branch__let_else__let_else *)
letfun new_tests__rustc_coverage__branch__let_else__let_else(value: bitstring) =
  let wildcard: bitstring = ((let bl__seq0 = (core__iter__traits__collect__IntoIterator__into_iter(
    core__ops__range__Range__Range(nat_lit(0), nat_lit(1))
  )) in 
  let rust_primitives__hax__array_cons(wildcard: bitstring, bl__seq1) = bl__seq0 in (let wildcard = (rust_primitives__hax__Tuple0__Tuple0) in
  let rust_primitives__hax__array_cons(wildcard: bitstring, bl__seq2) = bl__seq1 in (let wildcard = (rust_primitives__hax__Tuple0__Tuple0) in
  let rust_primitives__hax__array_cons(wildcard: bitstring, bl__seq3) = bl__seq2 in (let wildcard = (rust_primitives__hax__Tuple0__Tuple0) in
  let rust_primitives__hax__array_cons(bl__xh, bl__xt) = bl__seq3 in bitstring_err() else (rust_primitives__hax__Tuple0__Tuple0)) else (rust_primitives__hax__Tuple0__Tuple0)) else (rust_primitives__hax__Tuple0__Tuple0)) else (rust_primitives__hax__Tuple0__Tuple0))) in
  let Some(x) = value in
    (let wildcard: bitstring = new_tests__rustc_coverage__branch__let_else__say(x) in
    rust_primitives__hax__Tuple0__Tuple0)
  else let wildcard: bitstring = new_tests__rustc_coverage__branch__let_else__say(
    string_lit__none
  ) in
  rust_primitives__hax__Tuple0__Tuple0.
(* src: src/rustc-coverage/branch/let-else.rs:30 new_tests__rustc_coverage__branch__let_else__main *)
letfun new_tests__rustc_coverage__branch__let_else__main(wildcard: bitstring) =
  let wildcard: bitstring = new_tests__rustc_coverage__branch__let_else__let_else(
    Some(string_lit__x)
  ) in
  let wildcard: bitstring = new_tests__rustc_coverage__branch__let_else__let_else(
    Some(string_lit__x)
  ) in
  let wildcard: bitstring = new_tests__rustc_coverage__branch__let_else__let_else(None()) in
  rust_primitives__hax__Tuple0__Tuple0.
(* File automatically generated by Hacspec *)
Set Warnings "-notation-overridden,-ambiguous-paths".
From Crypt Require Import choice_type Package Prelude.
Import PackageNotation.
From extructures Require Import ord fset.
From mathcomp Require Import word_ssrZ word.
(* From Jasmin Require Import word. *)

From Coq Require Import ZArith.
From Coq Require Import Strings.String.
Import List.ListNotations.
Open Scope list_scope.
Open Scope Z_scope.
Open Scope bool_scope.

From Hacspec Require Import ChoiceEquality.
From Hacspec Require Import LocationUtility.
From Hacspec Require Import Hacspec_Lib_Comparable.
From Hacspec Require Import Hacspec_Lib_Pre.
From Hacspec Require Import Hacspec_Lib.

Open Scope hacspec_scope.
Import choice.Choice.Exports.

From RecordUpdate Require Import RecordUpdate.

Import RecordSetNotations.

Obligation Tactic := (* try timeout 8 *) solve_ssprove_obligations.

(*Not implemented yet? todo(item)*)

(*Not implemented yet? todo(item)*)

Equations say (message : both chString) : both 'unit :=
  say message  :=
    letb _ := black_box message in
    ret_both (tt : 'unit) : both 'unit.
Fail Next Obligation.

Equations let_else (value : both (t_Option chString)) : both 'unit :=
  let_else value  :=
    run (letb _ := foldi_both_list (f_into_iter (Build_t_Range (f_start := ret_both (0 : int32)) (f_end := ret_both (1 : int32)))) (fun _ =>
      ssp (fun _ =>
        ret_both (tt : 'unit) : (both 'unit))) (ret_both (tt : 'unit)) in
    matchb value with
    | Option_Some_case x =>
      letb x := ret_both ((x) : (chString)) in
      ControlFlow_Continue (letb _ := say x in
      ret_both (tt : 'unit))
    | _ =>
      letb _ := say (ret_both (none : chString)) in
      letm[choice_typeMonad.result_bind_code 'unit] _ := ControlFlow_Break (ret_both (tt : 'unit)) in
      ControlFlow_Continue (ret_both (tt : 'unit))
    end) : both 'unit.
Fail Next Obligation.

Equations main (_ : both 'unit) : both 'unit :=
  main _  :=
    letb _ := let_else (Option_Some (ret_both (x : chString))) in
    letb _ := let_else (Option_Some (ret_both (x : chString))) in
    letb _ := let_else Option_None in
    ret_both (tt : 'unit) : both 'unit.
Fail Next Obligation.

(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__unicode__v_申し訳ございません(wildcard1 : bitstring) =
       core_models__hint__black_box(false).

letfun new_tests__rustc_coverage__unicode__v_サビ(wildcard2 : bitstring) =
       ().

letfun new_tests__rustc_coverage__unicode__main(wildcard8 : bitstring) =
       let wildcard7: bitstring = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 for _İ in (core_models::iter::traits::collect::f_into_iter(
 core_models::ops::range::impl_7__new::<char>('А', 'Я'),
 )) {
 Tuple0
 }
 }"
         )
       ) in
       let wildcard6: bitstring = (
         if
           new_tests__rustc_coverage__unicode__v_申し訳ございません(()) && new_tests__rustc_coverage__unicode__v_申し訳ございません(
               ()
             )
         then
           (let wildcard5: bitstring = (
              std__io__stdio__e_print(
                core_models__fmt__rt__impl_1__new_const(["true
"])
              )
            ) in
            let wildcard4: bitstring = (()) in
            ())
         else (())
       ) in
       let wildcard3: bitstring = (new_tests__rustc_coverage__unicode__v_サビ(())) in
       ().

letfun new_tests__rustc_coverage__unicode__v_他(wildcard9 : bitstring) =
       ().


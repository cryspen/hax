(*****************************************)
(* Preamble *)
(*****************************************)

channel c.

fun construct_fail() : bitstring
reduc construct_fail() = fail.

type Option.
fun Some(bitstring): Option [data].
fun None(): Option [data].
letfun Option_err() = let x = construct_fail() in None().

const empty: bitstring.
letfun bitstring_default() = empty.
letfun bitstring_err() = let x = construct_fail() in bitstring_default().

letfun nat_default() = 0.
fun nat_to_bitstring(nat): bitstring.
letfun nat_err() = let x = construct_fail() in nat_default().

letfun bool_default() = false.


(*****************************************)
(* Types and Constructors *)
(*****************************************)



(*****************************************)
(* Functions *)
(*****************************************)

letfun new_tests__rustc_coverage__overflow__might_overflow(to_add : nat) =
       let wildcard7: bitstring = (
         if core_models__cmp__f_gt(to_add, 5)
         then
           (let wildcard6: bitstring = (
              std__io__stdio__e_print(
                core_models__fmt__rt__impl_1__new_const(
                  ["this will probably overflow
"]
                )
              )
            ) in
            let wildcard5: bitstring = (()) in
            ())
         else (())
       ) in
       let add_to = (
         core_models__ops__arith__f_sub(core_models__num__impl_u32__MAX, 5)
       ) in
       let args = ((add_to, to_add)) in
       let args = (
         [core_models__fmt__rt__impl__new_display(args.0),core_models__fmt__rt__impl__new_display(
           args.1
         )]
       ) in
       let wildcard4: bitstring = (
         std__io__stdio__e_print(
           core_models__fmt__rt__impl_1__new_v1(
             ["does "," + "," overflow?
"], args
           )
         )
       ) in
       let wildcard3: bitstring = (()) in
       let result = (core_models__ops__arith__f_add(to_add, add_to)) in
       let wildcard2: bitstring = (
         std__io__stdio__e_print(
           core_models__fmt__rt__impl_1__new_const(
             ["continuing after overflow check
"]
           )
         )
       ) in
       let wildcard1: bitstring = (()) in
       result.

letfun new_tests__rustc_coverage__overflow__main(wildcard8 : bitstring) =
       let countdown = (10) in
       let countdown = (
         rust_primitives__hax__failure(
           "Explicit rejection by a phase in the Hax engine:
a node of kind [Loop] have been found in the AST

Note: the error was labeled with context `reject_not_in_ProVerif`.
",
           "{
 (loop {
 |countdown| {
 (if core_models::cmp::f_gt(countdown, 0) {
 {
 let _: tuple0 = {
 (if core_models::cmp::f_eq(countdown, 1) {
 {
 let result: int = {
 new_tests::rustc_coverage__overflow::mi..."
         )
       ) in
       ().


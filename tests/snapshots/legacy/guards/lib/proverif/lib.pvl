(*
  Run with:
    proverif -lib <hax>/hax-lib/proof-libs/proverif/primitives.pvl lib.pvl

  primitives.pvl ships with hax and declares the symbols this file
  uses but does not define: the public channel c, the construct_fail
  sink, bitstring_default and bitstring_err, the Some/None and
  True/False constructors, the nat_N literal constants, logical_and
  and logical_or, and the rust_primitives, core, and hax_lib opaque
  functions.

  A symbol this file uses that is defined neither here nor in
  primitives.pvl is listed in missingdecl.pvl, a diagnostic that is
  not part of the model. Define each such symbol — by hand or from
  another crate's extraction — or leave it abstract as an
  ideal-functionality boundary.
*)

(* src: src/legacy/guards/lib.rs:5 new_tests__legacy__guards__lib__if_let_guard *)
letfun new_tests__legacy__guards__lib__if_let_guard(x: bitstring) =
  let None() = x in nat_0
  else let Some(v) = x in
    (let y = v in y
    else let Some(core__result__Result__Err(y_1: bitstring)) = x in y_1
    else nat_1)
  else let Some(core__result__Result__Err(y_1: bitstring)) = x in y_1
  else nat_1.
(* src: src/legacy/guards/lib.rs:14 new_tests__legacy__guards__lib__equivalent *)
letfun new_tests__legacy__guards__lib__equivalent(x: bitstring) =
  let None() = x in nat_0
  else let Some(y_1) = let Some(v) = x in (let y = v in Some(y))
  else None() in y_1
  else let None() = let Some(v) = x in (let y = v in Some(y))
  else None() in
    (let Some(core__result__Result__Err(y_2: bitstring)) = x in y_2
    else nat_1)
  else bitstring_err().
(* src: src/legacy/guards/lib.rs:33 new_tests__legacy__guards__lib__multiple_guards *)
letfun new_tests__legacy__guards__lib__multiple_guards(x: bitstring) =
  let None() = x in nat_0
  else let Some(v) = x in
    (let Some((=nat_1)) = Some(rust_primitives__hax__machine_int__add(v, nat_1)) in nat_0
    else let Some(v_1) = x in
      (let y = v_1 in y
      else let Some(core__result__Result__Err(y_1: bitstring)) = x in y_1
      else nat_1)
    else let Some(core__result__Result__Err(y_1: bitstring)) = x in y_1
    else nat_1)
  else let Some(v_1) = x in
    (let y = v_1 in y
    else let Some(core__result__Result__Err(y_1: bitstring)) = x in y_1
    else nat_1)
  else let Some(core__result__Result__Err(y_1: bitstring)) = x in y_1
  else nat_1.
(* src: src/legacy/guards/lib.rs:43 new_tests__legacy__guards__lib__if_guard *)
letfun new_tests__legacy__guards__lib__if_guard(x: bitstring) =
  let Some(v) = x in
    (let (=True()) = rust_primitives__hax__machine_int__gt(v, nat_0) in v
    else nat_0)
  else nat_0.
[package]
name = "proverif-inline"
version = "0.1.0"
edition = "2021"

[dependencies]
hax-lib = { path = "../../hax-lib" }

[package.metadata.hax-tests]
into."proverif" = { broken = false, snapshot = "none" }
